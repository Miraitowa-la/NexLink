# NexLink 已知问题与限制

本文档是 NexLink 当前已知问题、技术限制和待改进事项的唯一记录位置。问题按优先级排列；优先级表示建议处理顺序，不代表每项都已在所有硬件和主机环境中复现。

## P0：安全与稳定性优先问题

### 配对过程缺少身份认证

当前配对 Offer 会以广播形式携带 LMK，RX 在配对状态下会接受符合格式的 Offer 并保存对端信息。配对窗口内的邻近攻击者可能监听密钥、抢先配对或伪造配对报文。

影响：攻击者可能获得无线调试链路访问权限，从而访问目标板的 SWD、JTAG、NRST 或 UART。

计划方向：采用双端物理确认、短码确认或设备身份密钥 challenge-response；配对过程不再明文传输 LMK。

### USB CMSIS-DAP v2 异常路径会终止程序

CMSIS-DAP v2 Bulk 端点在 DAP 处理失败、端点重提交失败或发送失败时仍存在 `abort()` 路径。USB 背压、主机异常、队列耗尽或无线链路异常时，设备可能 panic 并重启。

计划方向：移除正常运行路径中的 `abort()`，改为可恢复错误、端点恢复、限频日志和错误计数。

## P1：无线业务可靠性问题

### 无线 CDC 的协议 MTU 与 ESP-NOW transport 不一致

`nexlink_protocol` 允许 220 字节 payload，而 `espnow_transport` 最多只接收和发送 128 字节总包。协议头占用 8 字节，因此当前单个无线业务 payload 的安全上限是 120 字节。

RX UART 的一段数据可能按 220 字节分片，导致超过 transport 上限的帧发送失败；当前部分调用路径没有处理发送失败，可能静默丢失 CDC 数据。

计划方向：统一协议 MTU，优先将单帧业务负载限制为 120 字节以下；增加 CDC 分片、失败处理和数据完整性测试。

### CDC 流控尚非端到端可靠传输

当前 TX→RX 方向使用 4-credit 基础窗口限制发送量，但 credit 不代表 UART 或 USB 已最终成功接纳数据。无线队列、TX 请求队列、USB RingBuffer 或目标 UART 缓冲区满时仍可能发生数据丢失。

计划方向：明确每一级队列的满载策略；在接收端真正接纳后再确认 credit；增加丢包、溢出、队列满和吞吐统计。

### 无线 DAP 重试耗尽时缺少显式失败响应

无线 DAP 请求超时后会最多重发三次。重试全部失败时，当前不会为 USB 主机合成对应的 CMSIS-DAP 错误响应，主机工具只能自行等待超时。

计划方向：定义链路故障状态与失败响应；在断链期间抑制或拒绝新事务；验证 Keil、OpenOCD 等工具的恢复行为。

### 无线 CDC 参数与控制线支持不完整

无线 RX 当前主要映射 7 位和 8 位数据位、1 位和 2 位停止位。5/6 位数据、1.5 位停止位以及 DTR/RTS 控制线尚未完整透传。

计划方向：完整映射 USB CDC line coding；明确拒绝不支持的配置；实现 `CDC_CONTROL_LINE` 的传输和目标端处理。

## P2：工程健壮性与可维护性问题

### NVS 初始化分散，异常时可能擦除整个分区

模式、配对和 ESP-NOW transport 分别初始化 NVS。当遇到 `ESP_ERR_NVS_NO_FREE_PAGES` 或 `ESP_ERR_NVS_NEW_VERSION_FOUND` 时，当前逻辑可能擦除整个 NVS 分区，导致配对、模式及同分区其他组件的数据同时丢失。

此外，当前 LMK 的静态存储未启用 NVS/Flash 加密。若攻击者能够直接读取 Flash，可能获得配对信息和密钥。

计划方向：集中 NVS 初始化与迁移策略；擦除前输出明确原因；量产版本启用合适的 Flash/NVS 加密和密钥管理策略。

### 模块以静态单例为主，初始化失败缺少回滚

无线桥、链路、ESP-NOW transport 和 USB 设备主要使用静态全局状态。部分任务、队列和信号量创建失败时，已经创建的资源不会回收；重复启动同一模块时也可能覆盖句柄或导致重复注册。

当前模式切换依赖重启，因此普通使用中风险较低；但后续实现运行时切换、自动恢复或测试重复初始化时需要解决。

计划方向：为各模块增加显式状态、重复启动保护和反向清理；必要时补充 `deinit()` 接口。

### Core 0 的负载需要压力测试

Wi-Fi、ESP-NOW、USB 设备、USB CDC、Bulk/HID 发送、链路与配对任务主要运行在 Core 0。CMSIS-DAP v2 Bulk 端点忙时还存在最长 100 ms 的忙等轮询。

计划方向：以弱信号、DAP 烧录和高吞吐 UART 同时运行的场景测量任务高水位、CPU 占用、ESP-NOW 成功率和响应时间；随后用事件或任务通知替换忙等，并视结果调整任务归属。

### 三种角色尚未使用独立构建配置

`main/CMakeLists.txt` 当前通过注释切换 `main_tx.c`、`main_rx.c` 和 `main_direct.c`，默认只会编译其中一个入口。误选角色可能导致烧录到不匹配的设备，未选角色的编译问题也不易及时发现。

计划方向：使用 Kconfig/CMake 互斥选项或独立 build profile，并在自动化检查中分别构建 TX、RX 与 DIRECT。

## P3：规范、可观测性与发布准备

### 无线协议的 wire format 依赖当前平台

协议头采用 packed 结构体直接复制，整数使用当前 CPU 端序。由于当前 TX 和 RX 均为 ESP32-S3，实际使用不受影响；但协议未明确规定端序，不利于移植、离线解析和长期兼容。

计划方向：规定显式 little-endian 编码；集中管理 MTU、窗口大小、超时和队列深度等配置。

### 返回值与运行统计不足

部分队列投递、ESP-NOW 发送和 RingBuffer 写入返回值被忽略。系统发生丢包、队列满、重传或超时时，缺少统一计数器和受限频率的诊断日志。

计划方向：建立链路、DAP、CDC 和队列统计接口，便于压力测试和现场定位。

### 开发配置与发布配置尚未分离

当前工程保留开发阶段的日志、断言和优化配置。部分注释仍包含已完成阶段的计划性描述。

计划方向：分别维护开发和发布配置；审核调试日志对时序的影响；同步更新注释、README 和接口约定。

### USB VID/PID 仅用于开发测试

当前 USB VID/PID 为 `0x303A/0x1002`，不应直接用于正式发布或商用产品。

计划方向：发布前使用项目拥有或获得授权的 VID/PID，并重新验证 Windows、Linux 和调试工具的枚举行为。

## 验证待办

以下测试尚应作为后续整改的验收条件：

- TX、RX、DIRECT 三种角色分别 clean build。
- OpenOCD/Keil 连续下载、复位、断链和重连测试。
- 弱信号条件下 DAP 与 CDC 并发压力测试。
- CDC 的边界长度、长时间字节一致性和主机暂停读取测试。
- USB 拔插、CDC 未打开、队列满和目标 UART 降速测试。
- NVS 满、版本迁移、掉电写入和配对清除测试。
- 配对窃听、伪造 Offer、伪造/重放 Accept 和抢配对端测试。
